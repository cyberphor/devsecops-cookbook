# Recipe 22: Deploy UDS Core
* [Setup](#setup)

## Setup
**Step 1.** Download the UDS CLI. 
```bash
wget -O uds https://github.com/defenseunicorns/uds-cli/releases/download/v0.30.0/uds-cli_v0.30.0_Linux_amd64 &&\
chmod +x uds &&\
sudo mv uds /usr/local/bin/
```
